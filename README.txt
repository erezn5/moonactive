Moon-Active
--------------------------------
Please follow below instructions in order to run the test:

unzip the suite using suitable ZIP software:
(if you don't have one you can navigate to https://www.win-rar.com/start.html?&L=0 and download it from there).

Please notice that env.properties file contains the configurations for parameters to run the test:
i. I set default 'DEVELOP' environment (in the future if we want to change it to another environment we can do it in ease)...
ii. I set default URL address.
--------------------------------

Please navigate to path reference'\httpOperator\pom.xml' and run the following command to run it
from your terminal/command line -> 'mvn clean' and then 'mvn test'

Enjoy!